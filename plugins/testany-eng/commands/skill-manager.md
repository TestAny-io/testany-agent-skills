---
description: 打开 SkillDock，使用本地 GUI 管理 Codex skills、plugins 和 marketplace 来源
argument-hint: "[可选：项目目录]"
---

# SkillDock

从本命令实际安装目录定位 `../skills/skill-manager/SKILL.md` 并读取，以该 skill 为唯一执行说明；不要假定固定 checkout、宿主工具或预置 plugin root 环境变量。

将 `$ARGUMENTS` 作为可选项目目录或用户补充要求。打开管理界面不等于授权修改实际技能；具体 GUI 操作沿用用户选择和后端能力边界。
