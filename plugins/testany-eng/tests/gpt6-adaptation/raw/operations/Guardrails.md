# Guardrails ledger-preview v1 / approved

Owner：有权工程 Owner，批准日期 2026-09-10。
这是既有隔离样例服务的有效运行规范，不在本轮建立或修改项目级默认规则。
部署需要值班工程师持有变更窗口许可；本轮只写手册，没有部署许可。步骤包含包校验、版本切换、重启、健康和固定样例校验、失败回滚。
验收：health/preview 连续 3 次成功，version 匹配目标；重启后 30 秒未健康或 preview 不符合约定则回滚。保留前两版目录，清理不在当前手册操作范围。
监控：每分钟检查 health；连续 3 次失败告警值班工程师；每日健康成功率目标 99.9%，单次 health 延迟阈值 500ms。日志通过 journalctl -u ledger-preview 读取，禁止写用户秘密。故障分类：包损坏、语法错误、进程未启动、端口占用、health 版本错误；先读日志和状态，再按批准回滚，不临时增加基础设施。
正式独立评审尚未安排；本轮可完成文档草稿和作者自检，不给发布批准。
