请补齐 ACME workspace 中 case A1B2C3D4 的运行配置。它已上传脚本包。使用 runtime 00000000-0000-4000-8000-000000000001，执行器 python，入口 main.py；把普通环境变量 MODE 设为 preview。保持现有 private 可见性、workspace 归属及已有其他环境变量。脚本包说明在 workspace/case-package.md。

配置完成后，请在这个指定 runtime 上做一次 case dry run 并查看结果。我明确授权这一次验证；该脚本只处理隔离样例，不写业务数据，不访问其他系统，不需要执行 pipeline。
